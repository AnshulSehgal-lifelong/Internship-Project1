Act as a senior software architect. Design a secure, scalable local file upload and document management system for an AI application. 

Please generate the complete boilerplate backend code, database schema, and directory layout based on the following technical requirements:

1. ARCHITECTURE & FOLDER STRUCTURE:
- Use a local filesystem directory structure nested by year and month to avoid OS performance issues (e.g., `/storage/uploads/YYYY/MM/`).
- Physically rename uploaded files to a unique UUID (e.g., `7b9e4a21-3c2b-4a1a.pdf`) upon saving to disk to prevent filename collisions.

2. METADATA & DATABASE SCHEMA:
Provide a database schema (SQL) for a `documents` table that tracks:
- System Fields: unique id (UUID), storage_path (absolute path on disk)
- File Metadata: original_name, file_size_bytes, mime_type
- Application Context: user_id, status (enum: 'uploaded', 'processing', 'indexed', 'failed')
- AI Metadata: vector_collection_id (to link with a vector database)
- Timestamps: created_at, updated_at

3. LIFECYCLE WORKFLOW IMPLEMENTATION:
Implement an API endpoint and background job logic handling this exact sequence:
- Upload: Stream the file safely to the server, validate file size/type, and save it to the nested directory using the UUID.
- DB Insertion: Insert the metadata into the database. Include error handling: if the DB write fails, delete the physical file from disk to prevent orphaned files.
- Background AI Trigger: Mock a background worker/event trigger that passes the 'storage_path' to an AI pipeline for text extraction and vector embedding.
- Clean Deletion: Implement a delete endpoint that removes the database record first, then safely unlinks/deletes the physical file from the local disk.

Please provide the implementation using python FastAPI and PostgreSQL.

Side note:
There will be 2 types of files uploaded in this app - Company Policies (frontend/knowlege-base), and resumes(frontend/recruiment) for hiring. So keep that in mind 